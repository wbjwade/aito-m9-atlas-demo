# AITO M9 Atlas 行为约定

范围来自用户参考截图：单页浏览、选择、显隐、结构展开与复位。无服务器数据写入。现有工作区其他应用不是该新项目的样式来源。

| 能力 | 规范所有者 | 行为 |
|---|---|---|
| Scrollbar | styles.css 全局 | 可见、颜色令牌、窄屏自然滚动 |
| 分组筛选 | App.jsx | 只筛列表，不改变模型显隐；会话状态 |
| 显隐 | m9-parts.js + App.jsx | 分类开关、隐藏全部、恢复全部；隐藏选中项时清除选择 |
| 部件选择 | viewer.js + App.jsx | 画布/部件列表等价；显示类别及原始节点名，不编造参数 |
| 展开 | model-state.js + viewer.js | 0–100原生range，方向键可控，几何示意分离非维修顺序 |
| 视角 | viewer.js | 旋转/缩放按钮及预设提供非拖拽替代 |
| 重置 | App.jsx | 显示全部、展开0、取消选择、初始视角、停止自动旋转 |
| 错误 | App.jsx | 加载失败可重试，不无限加载；无WebGL明确说明 |

无需CRUD、日期/选择弹层、账号、持久化或授权流程。用户提供的 M9 模型在本地使用，未发布到外网。原始资产许可为 CC BY-NC-SA 4.0，署名保留在 public/models/aito-m9 和应用来源说明中。

显示计数是 54 个命名模型部件（235 个网格实例），不是工程 BOM。四轮来自源文件轮组模板的展示性复制装配，原模型没有精确轮心定位，未定位车牌不展示。无独立电池、电机、悬架模型，不提供相应虚构分类。分类过滤只改左侧列表，不隐藏车辆部件。
