# M9 导入验收记录

日期：2026-09-08。范围：将用户提供的 `aito-m9.zip` 导入现有结构浏览器；保持原界面布局，不修改轻记，不部署到外网。

## 实现与来源

- 运行入口：`http://127.0.0.1:4174/`。
- 主模型：`public/models/aito-m9.glb`，13,232,360 字节，内嵌贴图，无外部模型/纹理请求、无 Draco 解码要求。
- 作者 MattDoesBlender，CC BY-NC-SA 4.0；原始文件及许可保留。源数据检查见 `m9-model-inspection.md`。
- 54 个可选总成，235 个网格实例，分类计数依次为 6/13/15/5/1/2/4/8。不是实车 BOM。
- 原文件只有一个轮胎及一个轮毂模板；运行时保留模板相对位置，复制为四组。展示性位置详见 `src/build-m9.js` 的 `WHEEL_PLACEMENTS`，不作为工程尺寸。未定位车牌模板不显示。
- 更新中文标签、品牌、模型说明、署名；保留原布局、字体尺寸及交互。按 Frontend Design Premium 的规范同步 DESIGN/UX 文档，并微调辅助文字令牌以修正分段控件对比度警告。

## 自动检查

- `npm run build`：通过，生成 `dist/client` 和原有静态服务适配产物。
- `npm test`：21/21 通过，其中 M9 专项测试 9 项。真实 GLB 经 Three GLTFLoader 与 `buildM9Parts` 验证，Node 仅以测试 Texture 代替浏览器 PNG 解码；原始 PNG 字节另外校验。
- Prettier：App、viewer、m9-parts、build-m9、styles 与新增测试文件检查通过。
- `designmd lint DESIGN.md`：0 错误、0 警告。
- Premium strict audit：0 findings，原始 JSON 结果保留在 `premium-audit-m9.json`。
- 仓库没有 TypeScript、独立类型检查或 Storybook 配置，未声称通过不存在的检查。

## 实际浏览器检查

使用本机内置浏览器，不是仅检查源码。截图和 DOM 证据见本次任务工具输出；未虚构截图磁盘路径。

- 桌面 1517×1004：默认完整装配、前视、侧视、俯视均显示正确；轮毂朝外、轮胎位于可见轮拱处。与 `docs/reference.png` 对比，保留左侧系统面板、右侧模型展台和底部展开控制器。
- 100% 展开：总成材质网格保持在一起，车轮/玻璃/前舱盖等分离，画面无整体裁切。
- 隐藏轮组：54 → 46；恢复后回到 54。
- 全部隐藏：计数 0，显示“所有部件已隐藏”和恢复按钮；恢复成功。
- 左前车门列表选择：显示中文名称及源节点 `gsraitom9_door_FL`；隐藏车门分类后计数 49，选择信息被清除。
- 底盘筛选：只过滤左侧列表为底盘护板和轮胎/轮毂，整车仍为 54 个可见部件。
- 重置：恢复完整装配、54 个部件、0% 展开和默认 3D 视角。
- 帮助对话框：包含 M9 限制和署名；Esc 关闭后焦点回到“使用指南”。
- 键盘滑杆 End 能设为 100%，控制器显示同步。
- 手机 390×844：完整装配与 100% 展开均可见，无横向溢出；模型在前、系统列表在后，正常文档滚动。
- 正常预览的浏览器错误/警告日志为空。
- 使用仅监听 127.0.0.1:4175 的临时 QA 代理使模型请求返回 503：显示“暂时无法显示模型”和“重新加载”，控件保持禁用；恢复代理请求后点击重试，成功回到 54 个部件。未移动或破坏模型文件，测试代理与测试页面在验收后关闭。

## 限制与后续风险

- 社区展示资产，不是官方工程结构。未提供独立电池、电机或悬架；不编造对应零件。
- 只有浏览器桌面与窄屏模拟验收，未在实体手机或所有 GPU 上验证。
- 构建仍有 Three.js 主包约 838 kB 的非阻断体积警告；模型 GLB 约 13.2 MB。当前本地使用，未进行公网弱网性能承诺。
- 本次未做完整读屏、高对比度系统主题、减弱动态偏好和多浏览器兼容认证。
- 未生成新的 APK/HAP；当前交付是已运行的本地浏览器版。
